# Intermedios regenerables: se reconstruyen con `python3 src/audit/run_all.py`
data/interim/

# Nunca desplegar la capa interna en la web pública (CLAUDE.md, <data_governance>).
# Se versiona en el repo de trabajo, pero queda excluida del build público de Fase 3.
# (la exclusión efectiva del bundle se implementa en Fase 3)

__pycache__/
*.py[cod]
.venv/
.DS_Store
